import React, { Component } from "react";
import { StyleSheet } from "react-native";
import { UIContainer, UITextView } from "../../components";

const LoginScreen = () => {
  return (
    <UIContainer>
      <UITextView text="Login screen" />
    </UIContainer>
  );
};

const styles = StyleSheet.create({});

export default LoginScreen;
